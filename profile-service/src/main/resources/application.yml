server:
  port: 8082

# Eureka Server
eureka:
  client:
    serviceUrl:
      defaultZone: "http://localhost:8761/eureka"

  instance:
    prefer-ip-address: true
    hostname: localhost

spring:
  application:
    name: profile-service